# Electrol = Electron + Control

Electron web 内容控制工具集，包含点击、输入等自动化功能。

- 部分 api 参考自 `Playwright`。

为什么不用 `Playwright`？

说实话在并编写这个的时候我还不知道 `Playwright` 的存在，编写过程中查阅了一些资料，发现 `Playwright` 提供了类似的功能。

出于实际需求只是想要一个更轻量级的解决方案，并且 `Electrol` 更加专注于 `Electron` 环境下的自动化操作，所以选择了自己实现。
